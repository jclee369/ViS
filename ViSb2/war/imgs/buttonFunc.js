function populateT(type){
	var box = document.getElementById("topmenu");
	var btn = document.createElement("BUTTON");
	element.setAttribute("onclick", selectTile());
	//for eachtile in type
	box.appendChild(btn);
}